export interface User {
    id: number;
    name: string;
    email: string;
    role: string;
};
export interface Userboard {
    name: string;
    email: string;
}

export interface Token {
    token: string;
}